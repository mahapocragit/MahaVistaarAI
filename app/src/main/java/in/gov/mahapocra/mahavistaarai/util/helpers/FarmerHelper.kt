package `in`.gov.mahapocra.mahavistaarai.util.helpers

import android.content.Context
import `in`.co.appinventor.services_api.settings.AppSettings
import `in`.gov.mahapocra.mahavistaarai.util.AppConstants

object FarmerHelper {

    fun containsFarmerId(context: Context): Boolean {
//        var isFarmer = false
//        val rawValue = AppSettings.getInstance().getSavedValue(context, AppConstants.AGRISTACKID)
//        val farmerId = if (rawValue.isNullOrEmpty() || rawValue == "null") "" else rawValue
//        if (farmerId != "") {
//            isFarmer = true
//        }
//        return isFarmer
        return false
    }

}