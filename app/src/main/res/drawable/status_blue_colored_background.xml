<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base white background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/gradient_top_figma" /> <!-- white background -->
        </shape>
    </item>

    <!-- Colored status bar area -->
    <item android:top="48dp"> <!-- We'll override this dynamically -->
        <shape android:shape="rectangle">
            <solid android:color="@color/white" /> <!-- pink or your desired color -->
        </shape>
    </item>

</layer-list>