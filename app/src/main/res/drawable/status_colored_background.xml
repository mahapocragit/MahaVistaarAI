<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Base white background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/actionbar_color_figma" /> <!-- white background -->
        </shape>
    </item>

    <!-- Colored status bar area -->
    <item android:top="@dimen/_38sdp"> <!-- We'll override this dynamically -->
        <shape android:shape="rectangle">
            <solid android:color="@color/white" /> <!-- pink or your desired color -->
        </shape>
    </item>

</layer-list>